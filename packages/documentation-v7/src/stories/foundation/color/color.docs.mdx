import { Meta } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import { BADGE } from '../../../../.storybook/constants';
import { forEach } from '../../../utils/react';
import { parse } from '../../../utils/sass-export';
import { ColorSwatch } from './color.blocks';
import scss from './color.module.scss';
import './color.styles.scss';

export const {
  brandColor,
  gray,
  contextualColor,
  pageBackground,
  accentColor,
} = parse(scss);

<Meta
  title="Foundations/Color"
  parameters={{ badges: [BADGE.BETA, BADGE.NEEDS_REVISION] }}
/>

# Color

In the following, you can see the Swiss Post specific colors.<br/>
They theme our elements and components.

<div className="alert alert-info alert-md mt-regular">
  For accessibility reasons, you should always make sure that your individually created color combinations are sufficiently contrasting. There may be a better solution than coloring elements individually.

  For example, use the <LinkTo kind="utilities-background--background-containers">background utilities</LinkTo> to get elements with a specific background color and the best possible contrast color for its content.
</div>

## Brand colors

<section className="color-swatch__section">
  {forEach(brandColor, color => {
    return <ColorSwatch key={color.key} name={color.key} color={color.value} />;
  })}
</section>

## Shades of gray

<section className="color-swatch__section">
  {forEach(gray, color => {
    return <ColorSwatch key={color.key} name={color.key} color={color.value} />;
  })}
</section>

## Page Backgrounds

<section className="color-swatch__section">
  {forEach(pageBackground, color => {
    return <ColorSwatch key={color.key} name={color.key} color={color.value} noCSS />;
  })}
</section>

## Signal colors

<section className="color-swatch__section">
  {forEach(contextualColor, color => {
    return <ColorSwatch key={color.key} name={color.key} color={color.value} />;
  })}
</section>

## Dark accent colors

<section className="color-swatch__section">
  {forEach(accentColor.dark, color => {
    return <ColorSwatch key={color.key} name={color.key} color={color.value} />;
  })}
</section>

## Bright accent colors

<section className="color-swatch__section">
  {forEach(accentColor.bright, color => {
    return <ColorSwatch key={color.key} name={color.key} color={color.value} />;
  })}
</section>

# Usage

## CSS

CSS variables are globally available on your page.

```css dark
.my-element {
  property: var(--post-dark);
}
```

## Sass

Import the color variables from the `core` file with `@use`. This way, the color variables are scoped and don't mix with your own variables.

```css dark
@use '@swisspost/design-system-styles/core' as post;

.my-element {
  property: post.$dark;
}
```
